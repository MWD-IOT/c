#include <stdio.h>
#include <string.h>

int func()
{

    return 0;
}

void main(void)
{
    // int len = 0, x, y, z;
    // x = y = z *2;
    // int num = 0x123456;
    // char *p = &num;
    // // int *p=0x10, *q = 0x20;
    // // char str1[] = {"string 1"};
    // // char str2[] = {'s', 't', 'r', 'i', 'n', 'g', '2'};
    // // char str3[16] = {0x33, 0x34, 0x00};
    // // strncpy(str3, str2, sizeof(str2));
    // // len = strlen(str3);
    // // printf("len=%d %d\n", len, sizeof("hello world"));
    // // printf("p-q=%p\n", p-q);
    // printf("p[0]:%x\n", p[0]);
    // printf("p[1]:%x\n", p[1]);
    // printf("p[2]:%x\n", p[2]);
    // printf("p[3]:%x\n", p[3]);
    // int a = func();
    // printf("a=%d\n", a);

    int x =15,y;
    float z = 12;
    x>0?y=x:y = -x;
    printf("%d\n",y);
}